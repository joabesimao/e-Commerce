export interface ContactsModel {
  phone: number;
  phoneSecundary: number;
  email: string;
}

export interface Contacts {
  id: number;
  phone: number;
  phoneSecundary: number;
  email: string;
}
